# Terraform 變數值示例文件
# 用途：複製此文件為 terraform.tfvars 並根據環境修改
# 注意：terraform.tfvars 應加入 .gitignore，不要提交到版本控制
# 說明：其他變數都有合理的預設值，通常不需要覆蓋

# AWS 配置
aws_region  = "us-east-1"  # 根據你的需求調整 region
aws_profile = "admin"      # 根據你的 AWS CLI profile 調整

# Key Pair 配置
key_pair_name   = "terraform-ec2"            # EC2 使用的 Key Pair 名稱
public_key_path = "~/.ssh/terraform-ec2.pub" # SSH 公鑰路徑

# 安全性配置
allowed_cidr_blocks = ["0.0.0.0/0"] # 允許 SSH 的 IP 範圍，建議改成你的 IP

# VPC 網段配置（若與現有 VPC 衝突時才需要調整）
# vpc_cidr_block      = "10.0.0.0/16"
# public_subnet_cidr  = "10.0.0.0/20"
# private_subnet_cidr = "10.0.16.0/20"
